def call(body){

def name = "SharedName"

pipeline{
   agent any
  stages{
     stage("current build number stage"){
       steps{
        echo "this is the current build number ${currentBuild.number}"
        echo "This is being ehoed from shared library groovy file"
        println "${name}" 
}
}
}
}

}